# 本地兼容更新

## v1.0.0-compat.15 — 2026-09-20

- 修复 Claude 兼容上游将用量放在 `message_stop` 时被遗漏的问题；原生 Claude 与 OpenAI 转换路径均在输出结束前合并最终用量，累计值不重复相加。
- 区分缺失用量与明确的零值。缺少字段时仅估算缺失部分并标记本地估算，保留缓存读取、缓存写入及 5 分钟/1 小时拆分；非流式成功消息沿用同一处理规则。
- 修复仅有缓存用量时结算被误清零的问题；无可计费用量时零结算并返还预扣，不再仅凭零 Token 标注“上游出错”。
- 流内错误仍优先判失败；只有用量更新但没有结束标志的响应不计为成功。HTTP 200 不代表 SSE 业务成功，历史内容策略拒绝不会改成成功或补扣费。
- 无数据库迁移，不修改渠道配置与生产服务；升级后生效。新增末帧解析、缺失/零值、缓存保留、错误/截断及解析到结算日志的回归测试。

## v1.0.0-compat.14 — 2026-09-20

- 适配上游 `972aed197`：日志模型差异改为根据请求、映射和上游声明名称实时判断，不再依赖历史 `mismatch` 标志；新日志仅保存三个名称，不修改历史数据库记录。
- 收紧匹配边界：忽略大小写和首尾空格，允许明确的供应商路径及合法 `YYYY-MM-DD` / `YYYYMMDD` 日期快照；任意前后缀、mini/nano/pro 等变体、无效日期仍显示差异，除非显式配置了对应模型映射。
- 保留流式首个不匹配声明、重试隔离和日志快照；前后端使用同一组 39 个边界样例，兼容旧日志的过期标志、缺失字段及新日志无标志格式。模型声明仍只作诊断，不改变路由、计费或响应内容。
- 升级需同步前后端；已打开的旧页面请刷新以加载新比较规则。无需数据库迁移，不涉及生产自动部署。
- 验证：全量 Go 测试、日志相关竞态测试、Go 构建、前端 20 项测试、定向 ESLint/格式检查及 Bun 生产构建通过。Playwright 本地模拟 API 核验桌面弹层、390px 暗色与刷新后的新旧日志提示；本机缺少 `/usr/bin/chromium`，公开页面预渲染按既有脚本跳过。

## v1.0.0-compat.13 — 2026-09-19

- 精简 Meta Pulse 用户页，移除本期账本、活动周期和本期贡献卡片，只保留等级、经验值、可用奖励券及奖励历史。
- 将用户页“贡献值”统一改为“经验值”，沿用累计数据和原有换算精度，完成七种语言文案同步。
- 概览调整为三列布局，窄屏自动纵向排列；接口、后台记账、等级计算与奖励结算规则保持兼容。
- 验证：前端格式检查、定向 ESLint、七种语言键完整性和 Bun 生产构建通过；Playwright 使用本地模拟 API 验证桌面、320/390px 窄屏、暗色、英文、刷新、旧字段兼容、空数据及服务异常提示。本机缺少 `/usr/bin/chromium`，公开页面预渲染按既有脚本跳过；Docker 构建继续执行该步骤。

## v1.0.0-compat.12 — 2026-09-19

本轮按功能选择性融合 QuantumNous/new-api 上游截至 `b0bf2580e` 的适用更新；不是完整同步上游主线。保留现有 Semi 前端、Pulse、钱包、独立令牌、分组健康、渠道重试和续传逻辑。

### 安全与稳定性

- 禁止 GitHub 旧账号绑定按可变用户名自动迁移，避免同名账号接管；数值 ID 绑定不受影响。旧用户名绑定须先用其他已绑定方式登录，再显式重新绑定。
- 修复未完成 Passkey 验证即可通过二次验证的问题；挑战与验证结果使用数据库原子消费的一次性凭据，拒绝重放、过期及跨用户/用途消费。新增 `verification_proofs` 表，凭据仅存摘要。
- 按协议终态判定流式成功，HTTP 200、仅收到 EOF 或部分输出不再自动视为成功；性能采样放在请求结束边界。保留本地分组健康的首个有效输出与缓存统计口径。
- 成功限流采用原子预占名额，失败释放、成功完成后计入时间窗口；内存和 Redis 均覆盖并发。Redis 在途租约可续期并在进程异常退出后过期回收。

### 计费与适配器

- Responses 流中断后，有效输出已交付时保留已发生用量并结算一次，优先使用上游 usage，缺失时才估算；仍标记调用失败。无有效输出不结算，不再对已结算请求二次退费。
- 图片 JSON 响应按真实 URL/Base64 载荷数结算，兼容单对象返回与转 SSE；空数据不视为成功。保留预扣上限、表达式计费快照、钱包/订阅/独立令牌路径。
- 补齐图片输出 Token 与缓存输入模态明细，区分字段缺失和显式零值，避免续传共享对象及重复累计。
- 修复 Ollama 结束帧丢失文本、思考和工具调用；补传 Gemini 转 OpenAI 的流式 usage 选项，拒绝未实现的 Gemini countTokens 路由以免误生成扣费。
- 保留智谱 reasoning_effort，兼容音频扩展名大小写；DeepSeek 余额统一为美元并校验异常值，Claude 工具结果名称按单次索引回填。

### 数据库与界面

- SQLite 使用真正生效的忙等待、WAL 和 immediate 写事务，保留用户显式 DSN 参数；PostgreSQL 关闭显式预处理语句以兼容事务连接池。
- 成组更新 SQLite/GORM/MySQL/PostgreSQL 驱动，规范 MySQL decimal 默认值和 PostgreSQL CHAR 比较；按真实名称迁移旧单列唯一约束。修复新版 GORM Scan 覆盖原费用汇总的问题。
- 修复 PostgreSQL 预填分组旧全局唯一约束与 JSON 写入；保留不相关索引，遇到外键依赖或目标定义异常时回滚，禁止级联删除。
- 日志展示请求、映射后及上游声明模型；声明差异只作诊断，不证明实际模型身份，也不改变计费/路由。补齐七种语言文案。
- 订阅消费行直接显示金额，使用小图标标注抵扣来源；真实零费用正常显示，失败日志仍以同 Request ID 的账单为准。

### 升级注意与验证

- 升级前备份数据库，排空在途请求；安全凭据与成功限流不宜和旧版长期混跑。Redis 成功窗口使用新命名空间，切换时旧的短期成功计数不继承。
- 全量 Go 测试、后端构建、定向竞态检测、20 项前端脚本测试、Bun 两套生产构建通过。
- SQLite、临时 MySQL 8.0 和 PostgreSQL 16 的迁移、重复启动、旧约束回滚、令牌迁移、图片钱包预扣、一次性验证并发测试通过；未声称验证 MySQL 5.7 或 PostgreSQL 9.6 实例。
- Playwright 使用本地模拟 API 验证模型提示、金额/零值/失败日志/旧字段兼容、390px 窄屏、暗色与刷新。开发模式仍有 Semi 既有弃用警告；本机预渲染因缺少 `/usr/bin/chromium` 按既有脚本跳过，Docker 构建另行执行预渲染。
- 不引入上游整套新前端、RelayKit、任务插件、WebSocket Responses 架构；完整 session/action 绑定 proof 与统一登录二次验证留待独立迁移。
- 本次发布不自动部署生产；跨过 compat.6 升级仍须遵循资金迁移说明。

## v1.0.0-compat.11 — 2026-09-19

- 分组下拉、当前分组摘要、模型广场分组健康及模型详情新增缓存命中率，继续保留分组备注与紧凑布局。
- 命中率采用成功请求的缓存读取 Token 总量除以输入 Token 总量，包含未命中请求；统一 OpenAI、Claude、Gemini 用量口径，缓存写入不计为命中。
- 失败尝试、本地估算用量和图像/音频计费单位不进入缓存统计；同组重试只记录最终结果，无有效数据及旧后端字段缺失显示“—”，真实零命中显示 0.00%。
- 增加三列聚合计数，沿用小时桶、分组/模型权限过滤及多实例幂等快照。旧记录不回填，部署后开始积累缓存数据；本次发布不自动部署生产。

## v1.0.0-compat.10 — 2026-09-19

- 令牌编辑页将分组表现压缩为选择框下方的一行摘要，只显示成功率、首字和趋势入口，取消默认展开的大卡片。
- 分组下拉项采用紧凑两行布局，保留名称、倍率和分组备注；备注与成功率、首字同行，长备注可悬停查看全文，窄屏自动换行。
- 统计周期、调用次数和统计口径收进信息按钮，选中分组的完整备注也可在其中查看；默认不显示“样本不足”。趋势仍可按需展开。
- 保留无数据、加载失败、自动分组及生图完成耗时的区分，不改变分组选择、计费或后端统计口径。本轮无数据库变更；生产部署需单独执行。

## v1.0.0-compat.9 — 2026-09-19

- 后台“系统设置 → 配置 Meta Pulse 对接”集中管理运行环境、消费日志保护、服务地址、用户侧/运营侧/发奖/撤销密钥、社区 SSO 与奖励限额，保存后当前实例直接生效。
- 奖励限额同时展示内部 quota 和 API 额度单位，沿用实际 quota_per_unit；默认不启用发奖，不预填奖励预算。
- 保留已有环境变量和旧后台配置；未修改的密钥继续沿用，页面只显示配置状态。支持按用途独立生成密钥、轮换旧密钥和显式清除，不重置数据库、SSO 或随机密钥。
- 使用仅超级管理员可访问的原子配置接口，校验独立密钥、额度、回调地址及生产环境 Redis；密钥不进入配置返回值、审计日志或本次保存的 SQL 日志。多实例保存从数据库最新配置合并，避免覆盖其他实例刚更新的字段。
- 本次不新增数据库表，其他实例按配置同步周期生效。升级仍需部署新版本；Pulse 和社区插件仍分别配置，发奖启用及生产部署不会随版本发布自动执行。跨过 compat.6 升级时仍须遵循资金迁移说明。

## v1.0.0-compat.8 — 2026-09-19

- 统一清理控制台数据看板、令牌、日志、渠道、订阅、模型、Pulse、设置等页面重复的 60px 顶部留白，并消除左侧导航叠加的 12px 间距。
- 切换页面时重置内容滚动位置，避免继承上一页的滚动位置后需要手动向上翻；左右两侧继续独立滚动。
- 表格按导航与页面内边距后的可用高度布局，适配手机动态视口，消除外层多余滚动。
- 操练场和嵌入聊天页统一使用导航下方的可用高度，聊天输入区随父容器伸缩；修正手机设置面板的固定定位。
- 本轮仅调整前端布局，不新增数据库变更。跨过 compat.6 升级时仍须遵循该版本的资金迁移说明。

## v1.0.0-compat.7 — 2026-09-19

- 修复钱包管理与模型广场重复预留顶部导航高度的问题，移除多余的 60px 顶部空白。
- 钱包管理使用控制台可用宽度，取消 1280px 固定上限与移动端整屏最小高度，减少两侧留白和不必要的滚动。
- 模型广场按导航下方的实际视口高度布局，搜索筛选保持可见，模型列表与供应商筛选区独立滚动；兼容移动端动态视口高度。
- 本轮仅调整前端布局，不新增数据库变更。跨过 compat.6 升级时仍须遵循该版本的资金迁移说明。

## v1.0.0-compat.6 — 2026-09-19

- 接通社区抽奖额度的安全接收：按奖励编号幂等到账，新奖励仅可用于调用，默认关闭发放。
- 增加单笔、单用户每日和全站每日额度硬上限，跨实例并发共用数据库计数；暂停不影响查询和已成功请求重放，撤销不会重置日限额。
- 分离发奖与撤销密钥及角色；封禁、删除和支付追回风险账户不能领取新奖励，奖励撤销不得造成负余额。
- 真实在线支付建立付费资格，普通同步钱包调用按原子预扣及结算凭证记录付费部分，供 Pulse 安全产券。历史余额、赠送、兑换码、订阅及暂不支持的调用类型不能推定为真实付费。
- 增加 SQLite 多连接、MySQL 8、PostgreSQL 16 并发限额、重复到账、支付归因、软删除和余额缓存回归。
- 本次包含数据库增量迁移。升级必须先排空旧实例请求与钱包批量任务，禁止新旧资金版本混跑；配置与支持范围见 docs/PULSE_FUNDING_PROVENANCE.md。发布不会自动部署或开启奖励。

## v1.0.0-compat.4 — 2026-09-18

- 默认开启调用失败日志，并提供运营设置开关；已有显式关闭配置继续有效。
- 统一记录已识别用户请求的最终失败，覆盖鉴权、限流、渠道分配、参数/额度校验与上游错误。自动重试成功不再产生失败行，最终失败保留最近 5 次渠道尝试供管理员排查。
- 支持 HTTP 200 响应开始后的显式流内错误，分别展示 HTTP 状态与失败状态，附请求 ID、错误码、耗时、重试等待时间及处理建议。
- 使用日志增加“调用失败”筛选、详情与“复制排查信息”；失败行不将缺失 usage 显示为零费用，实际账单以同请求 ID 的消费或退款记录为准。
- 普通用户的错误日志采用公开字段白名单，隐藏渠道、上游原始诊断与敏感信息；旧错误记录保留历史重试提示，不推断最终结果。
- 补充 HTTP 200 流内错误、提前拒绝、重试恢复、开关、用户隔离与复制内容回归测试；不涉及数据库结构变更。历史未记录的失败不会自动补回，异步任务执行结果继续在任务日志查看。

## v1.0.0-compat.3 — 2026-09-18

- 修复 Vertex AI 视频任务完成后无法下载 Base64 视频的问题；支持带填充与不带填充的 Base64，并正确返回视频 MIME 类型。
- Vertex 视频任务保存创建时实际使用的密钥，轮询和下载继续使用该密钥，避免多密钥渠道查询错账号。
- Base64 视频任务保存本站下载代理地址；下载时识别并跳过自身代理地址，任务数据已清理或截断时重新查询上游结果。
- 下载查询沿用渠道并发限制与上游 429 冷却机制，拒绝未完成、失败及损坏的视频响应。
- 补充内容代理、原密钥持久化、轮询结果地址与错误处理回归测试。

## v1.0.0-compat.2 — 2026-09-18

- 将已有 cn-tunnel 分支中剩余的 3 个 Pulse 配置与说明提交合入 main，统一主分支更新入口。
- 管理面板支持配置 Pulse 内部地址、用户与运营 HMAC 密钥，并生成随机密钥；未配置时继续使用环境变量。
- 补充 Pulse 密钥同步与运营概览配置说明。
- 本次主分支发布同时包含此前已合入的渠道监控、日志统计优化与保留期清理、Pulse 运营概览，以及站点地址和余额划转修复。
- 沿用 main 的 Docker 发布与 newapi-update 更新流程。

## v1.0.0-compat.1 — 2026-09-13

在 nanashiwang/new-api 的本地 main 定制版本上，选择性适配 QuantumNous/new-api 上游修复。本版本不是上游 rc.37 的完整同步；保留现有前端、Pulse、支付、钱包、风控、渠道及令牌套餐实现。

- 优化 RawMessage 深拷贝：批量复制原始 JSON 字节，保留请求与重试间独立存储。来源：2cf177ac4。
- 修复火山方舟普通渠道模型列表端点为 `/api/v3/models`，保留特殊接入地址规则。来源：876903a8e。
- 按具体模型和推理模式处理 GPT Chat 参数，支持 GPT-6 Astra 及日期快照，保留兼容模型支持的采样参数，并同步渠道测试和系统角色判断。未知模型不自动继承这些限制。来源：49ec46966。
- 校验图片数量和实际出站数量，覆盖 JSON、multipart、Ali `parameters.n`、Imagen `sampleCount`、渠道参数覆盖及重试。来源：f064bffa2，按本地计费架构重写。
- 图片按最终数量在发送前预扣；钱包使用三库兼容的原子余额条件更新，避免余额不足仍发起生成。保留分组与时段倍率、DALL-E 尺寸/质量价格、订阅及独立令牌计费。补扣失败回滚资金，不向上游发起请求。
- 表达式计费读取校正后的图片数量，保留其余请求上下文；按 token 的表达式不额外乘图片数量。
- 为现有三平台 Release 工作流增加指定版本的手动草稿入口，并校验版本与 VERSION 一致。

### 行为变化

- 图片数量上限为 128。标准 `n` 缺省或为 0 时按 1 处理；Ali `parameters.n` 显式为 0、负数、非整数或超限时拒绝。
- 图片请求在发送前验证可用额度，普通钱包的信任额度旁路不再跳过这一步。已有余额不足的请求可能更早返回 403。
- 没有数据库结构变更，也不启用上游 Task Plugin、RelayKit 或新的前端体系。

### 验证

- Go 全量回归测试通过，完整后端构建通过。
- 新增请求隔离、模型参数、图片数量、预扣/结算/退款测试及定向竞态检测通过。
- SQLite、MySQL 8.0、PostgreSQL 16 的图片钱包预扣、返还与并发余额保护实测通过；未声称验证 MySQL 5.7 或 PostgreSQL 9.6 实例。
- Bun 前端与图片 Playground 生产构建通过。预渲染因本机未安装 `/usr/bin/chromium` 按现有脚本跳过，继续使用后端元信息渲染兜底。
- `go vet ./...` 存在原始代码同样的 16 条锁复制/不可达代码诊断；未新增这两类诊断，其他检查通过。
- Apple M5 上 256 KiB RawMessage 微基准：约 9.90 ms → 0.026 ms，262159 → 17 次分配。仅代表该微基准，不代表整站吞吐提升。
