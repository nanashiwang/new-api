# 更新日志

## 2026-03-08

### 新增功能

#### 用户管理页套餐操作补齐（复用与一致性）
- 用户订阅管理中“购买数量”改为受动态可购上限限制：会按 `purchase_quantity_max - active_quantity_by_plan` 计算，避免出现“上限 10 仍可选 12”。
- 点击“新增订阅”前新增确认弹窗，降低误操作风险。
- 管理端新增订阅购买方式与购买页对齐：支持 `stack / renew / renew_extend`，并支持数量大于 2 时一次续费多个月（同目标顺延）。
- 管理端订阅列表接口返回结构升级为 `{ subscriptions, active_quantity_by_plan }`，前端兼容旧数组格式。
- 抽离前端公共规则到 `subscriptionPurchase` helper，统一购买页与用户管理页的数量规则和可续费筛选逻辑，减少重复实现和后续漂移。

#### 国际化补充（订阅管理相关文案）
- 补齐本次新增/复用文案在 `zh-CN / zh-TW / en / fr / ru / ja / vi` 的翻译映射，避免非中文界面回落中文键值。

## 2026-03-07

### 功能改进

#### 令牌管理支持“套餐令牌”（复用原添加入口）
- 复用“添加令牌/编辑令牌”入口，新增“标准令牌 / 套餐令牌”切换，不新增独立页面。
- 套餐令牌支持周期配置：每日 / 每周 / 每月 / 自定义秒数。
- 套餐预算改为金额心智输入（非技术额度字段外露），并保留后端额度扣减逻辑不变。
- 令牌列表支持套餐信息识别与筛选（标准/套餐）。

#### 套餐用量可视化（不增加表格拥挤度）
- 在“剩余额度/总额度”悬浮信息中新增套餐维度信息：
  - `本周期剩余`
  - `下次重置时间`
- 保持表格列结构不变，不新增列，避免管理页变臃肿。

### 安全与一致性修复

#### 套餐额度关系校验加固
- 新增关系校验：非无限额度 + 套餐令牌时，要求总额度不小于套餐周期额度。
- 关系校验仅在创建/更新令牌时执行，避免运行时扣费链路误触发。
- 关系口径按“总额度 = 可用额度 + 已用额度”，避免“正常消耗后被误判非法配置”。

#### 金额输入口径统一（防止币种语义漂移）
- 令牌管理中的“总额度金额/周期金额”统一按 USD 语义输入与换算，不再随前端展示币种变化。
- 令牌金额输入与系统内部额度换算保持固定口径，降低输入歧义风险。

## 2026-03-05

### 功能改进

#### 邀请返佣口径补齐（套餐/兑换码）
- 套餐支付成功纳入邀请返佣，支持 `首购` 与 `续费`。
- 兑换码充值纳入邀请返佣；管理员直接加余额不参与返佣。
- 套餐返佣按实付折算：`base_quota = floor(money * QuotaPerUnit / Price)`，再按返佣比例计算。
- 返佣链路补齐事务一致性、幂等去重与防篡改校验。

## 2026-03-04

### 功能改进

#### 订阅购买与上限规则
- 支持购买方式手动选择：`叠加` / `续费`；续费仅允许同规格套餐（同 `plan_id`）。
- 选择续费时不再自动回退为叠加；无可续费目标会直接提示失败。
- 上限拆分为两个独立字段：`购买上限`（总购买次数）与 `叠加上限`（同时生效叠加数）。
- 新增订阅购买数量范围配置（管理员可改），默认 `1~12`，前后端按该范围统一校验。
- `最大购买数量`按“当前未过期份数”动态扣减可买上限（会随到期自动回升）。

#### 安全与一致性修复（订阅支付链路）
- 在订单完成（支付回调）阶段增加二次校验：动态可买上限、购买上限、叠加上限都会再次校验，避免并发绕过。
- 完成订单事务中增加用户级串行锁，降低同用户并发支付造成的超卖风险。
- 前端动态可买数量改为使用后端返回的 `active_quantity_by_plan`，避免前后端算法漂移导致显示与校验不一致。

#### 渠道重试优化

- 把原本失败尝试的值从`0`改成了`1`
- 失败`1`次后会尝试其他渠道
-   公式：总尝试次数 = 失败上限 + 1 , 所以：2 + 1 = 3

## 2026-03-02

### BUG修复

#### 用户备注保存修复
- 修复用户管理“新增用户”时 `remark` 未写入数据库的问题。
- 修复后，管理员在创建用户时填写的备注可在后续编辑页面正常回显。

### 功能改进

#### 用户与令牌管理联动优化
- 新增用户与令牌的批量启用、批量禁用、批量删除能力，减少重复操作成本。
- 批量操作支持“部分成功”反馈，可明确看到成功与失败数量，便于后续处理异常项。
- 用户列表与令牌列表的批量交互体验优化：选择、执行、刷新流程更顺畅。
- 批量删除增加确认步骤，降低误操作风险。

#### 邀请关系收益提示可读性修复
- 邀请关系页补充收益相关展示，支持更直观查看直接收益、充值返佣与总收益。
- 被邀请人列表新增余额与产生收益展示，信息表达更完整。
- 修复“产生收益”悬浮提示换行问题，提升阅读连贯性。

#### 国际化工具链补充
- 优化多语言配置，补齐中文语言标识，提升语言资源管理一致性。
- 新增国际化审计能力，用于辅助排查翻译缺失、未使用项与可疑硬编码文本。
- 提升 i18n 日常维护效率，降低多语言迭代成本。

## 2026-03-01

### BUG修复

#### 邀请人数统计一致性修复
- 修复 `aff_count` 在并发注册场景下可能少计的问题：由“先查后改再保存”改为数据库原子自增。
- 修复邀请人数统计对 `QuotaForInviter > 0` 的错误依赖，保证关闭固定邀请奖励时仍会累计邀请人数。

#### 用户管理加载状态修复
- 修复用户页首屏命中“已保存筛选条件”时可能持续转圈的问题。
- 搜索分支补齐 `loading` 状态收敛，异常场景也会在 `finally` 中正确结束加载态。

### 新增功能

#### 邀请体系支持“双机制并存”
- 保留老机制：邀请注册固定奖励（`QuotaForInviter` / `QuotaForInvitee`）。
- 新增新机制：邀请充值返佣（按充值额度比例计算，`T+1` 结算）。
- 两种机制可同时存在、互不冲突。
- 返佣只统计成功充值订单，注册赠送额度不计入返佣。
- 返佣结算时间统一为自然日 `00:00`
- 月卡每日重置时间统一为自然日 `00:00`。
- 修改文件在`constant.go`

### 功能改进

#### OpenAI 渠道重试优化
- 改进前：用户配置多个 OpenAI 渠道时，遇到 `We're currently experiencing high demand`（429），重试可能再次选到刚失败的渠道，切换效果不稳定。
- 改进后：重试选渠道时会带上“已失败渠道 ID 列表”，并从候选中排除这些渠道，避免重复命中失败渠道。
- 改进效果：在多 Key 场景下，429 后的重试更容易切换到可用渠道，提升请求成功率和稳定性。

#### 用户管理与令牌管理优化
- 用户管理筛选增强：支持角色、状态、邀请人 ID、被邀请人 ID、是否有邀请人、是否有被邀请人。
- 支持邀请关系详情：新增接口 `GET /api/user/:id/invite-relations`，并提供侧栏查看当前用户、邀请人、被邀请人（分页）。
- 邀请信息补全：返回 `inviter_username`，避免前端逐行查询造成 N+1 问题。
- 用户/令牌列表均新增 `已使用余额`（`used_quota`）展示与区间筛选。
- 用户管理排序升级：支持 `ID` 与 `额度` 独立排序且可组合使用（如 `ID降序 + 额度升序`）。
- 用户管理筛选布局优化：`额度最小值/最大值` 移入高级筛选，主筛选区更紧凑；搜索提示统一为 `ID/用户名`。
- 令牌管理新增按分组筛选，金额筛选与金额升降序可联动查询，并在翻页/改每页条数/刷新时保持条件。
- 额度筛选语义统一为“总额度”（可用额度 + 已使用额度）；高级筛选分组与文案优化，移除内部字段名外露。
- 新增修复接口 `POST /api/user/rebuild-aff-count`，支持全量重算或按 `user_id` 单用户重算，修复历史 `aff_count` 不一致。

#### 额度稳定性与多 Key 防抖优化
- 新增同渠道多 Key 快速切换：当请求命中“额度类错误”且渠道为多 Key 时，同次请求可在同渠道内立即切换下一个 Key 重试一次，减少“这次失败下次又好”的抖动。
- 新增多 Key 冷却机制：触发额度类错误的 `channel + key_index` 会进入短时冷却（默认 `60s`），冷却期内不再参与选 Key。
- 新增 token 维度的 Key 粘性：同一 `token + channel + model` 在短窗口内优先复用同一上游 Key（默认 `60s`），减少来回切换上游账号导致的波动。
- 新增额度不足前 DB 二次确认开关：仅在即将返回“额度不足”时回源数据库复核，降低缓存短暂不一致导致的误判。
- 优化失败退款一致性：失败链路的额度返还改为优先立即生效，降低“先扣后还”的时间窗口。
- 新增可配置项（可在系统选项中调整）：
  - `QuotaStabilityEnabled`：是否开启额度防抖总开关。默认 `true`。
  - `QuotaInsufficientDBRecheckEnabled`：返回“额度不足”前是否回源 DB 二次确认。默认 `true`。
  - `MultiKeySameChannelFailoverOnce`：额度类错误时，是否在同渠道内立即切换 Key 重试一次。默认 `true`。
  - `MultiKeyCooldownSeconds`：触发额度类错误后，Key 冷却时长（秒）。默认 `60`。
  - `MultiKeyStickySeconds`：同一 `token + channel + model` 的 Key 粘性时长（秒）。默认 `60`。
