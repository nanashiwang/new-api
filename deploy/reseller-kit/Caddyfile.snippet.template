# 副站反代站点（由 provision.sh 用 envsubst 渲染 → ${CADDY_SITES_DIR}/${SUBDOMAIN}.caddy）
# Caddy 会自动为该子域名签发 / 续期 Let's Encrypt 证书。
# 高频开通可改用通配证书 *.你的域名.com（需 DNS-01，见 README）。
#
# 反代目标 = 副站容器在宿主上仅绑 127.0.0.1 的端口。
# 【退出迁移钩子】代理商下线并把用户迁回主站后，teardown.sh --retarget 会把下面这行
# 改成指向主站端口，从而旧子域名 + 旧 key 无感继续可用（详见 README / 规划文档 §6.3）。
${SUBDOMAIN} {
	reverse_proxy 127.0.0.1:${SUBSITE_PORT}
}
