# 本地定制更新

## v1.0.0-cn.1 — 2026-09-15

本版本发布 `codex/cn-tunnel` 分支中的站点地址适配、余额划转一致性修复及相关本地定制更新。CN 隧道安装包仍使用独立的 `cn-tunnel-*` 版本和发布流程。

- 修复邀请收益划转后的钱包余额缓存同步：划转完成后按本次额度原子增加 Redis 余额，不再用数据库快照覆盖缓存，避免吞掉尚未批量落库的并发调用扣费。
- 增加 Redis 回归测试，覆盖数据库余额、待使用收益、可划转余额、缓存差额、缓存 TTL 和其他用户缓存字段。
- 桌面客户端下载地址和复制邀请链接改为跟随后台“服务器地址”配置，不再固定使用 CN 域名。
- 修复 Docker 多架构发布中的凭据处理和构建失败问题。
- 包含鹈鹕骑自行车 SVG 动画资源。

### 行为变化

- 邀请收益仍按原额度 1:1 划入钱包；只调整划转完成后的缓存同步方式，没有手续费、倍率或数据库结构变化。
- 当 Redis 中存在尚未落库的余额扣减时，划转会保留该扣减差额，并只增加本次划转额度。

### 验证

- `go test ./model ./controller` 通过。
- `go test ./...` 通过。
- 邀请收益缓存并发场景的定向竞态测试通过。
- Bun 前端生产构建通过；本机未安装 `/usr/bin/chromium`，预渲染按现有脚本跳过并继续使用后端元信息渲染兜底。
